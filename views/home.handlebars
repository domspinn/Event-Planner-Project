    <style>
        body {
            background-color: #2c3e50;
            color: #ecf0f1;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            max-width: 400px;
            width: 100%;
            padding: 20px;
            text-align: center;
            background-color: #34495e;
            border-radius: 10px;
            box-shadow: black;
        }
        a {
            color: #3498db;
        }
        a:hover {
            color: #2980b9;
        }
    </style>

    <div class="container">
        <h1 class="title has-text-white">Welcome to {{title}}</h1>
        <div class="buttons is-centered">
            <a href="/auth/google" class="button is-ghost">Google Login</a>
        </div>
        <div class="buttons is-centered">
            <a href="/login" class="button is-primary">Login</a>
            <a href="/register" class="button is-link">Register</a>
        </div>
    </div>